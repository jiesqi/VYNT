CREATE TABLE IF NOT EXISTS user_seo (
  open_id TEXT PRIMARY KEY,
  video_id TEXT,
  seo_score INTEGER,
  seo_breakdown TEXT,
  hooks TEXT,
  keywords TEXT,
  hashtags TEXT,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
